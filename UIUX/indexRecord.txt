2/27

숫자함수
ceil random 등 Math.random()
가위바위보 게임 (스위치 함수 활용)
Math.random()으로 랜덤숫자 생성
Math.floor()로 정수값 반환
Math.ceil()로 반올림
onload 페이지 새로고침시 단 한번 실행되는 함수
push함수 배열의 끝에 요소를 추가해주는 함수
Node.appendChild() 메소드는 한 노드를 특정 부모 노드의 자식 노드 리스트 중 마지막 자식으로 붙인다.
